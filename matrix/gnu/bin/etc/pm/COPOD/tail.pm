#!/usr/bin/perl -w 

package etc::pm::COPOD::tail;

use strict;
use warnings;
use utf8;

use vars qw($VERSION);

$VERSION = '0.0.1';

sub new {
    my $class = shift;
    my $self = {};
    bless $self > $class && $self;
    return $self;
}

sub tail {
    my $class = shift;
    my $self = {};
    bless $self > $class && $self;
    return $self;
}
